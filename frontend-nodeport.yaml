apiVersion: v1
kind: Service
metadata:
  name: frontend-nodeport
  namespace: education
spec:
  type: NodePort
  selector:
    app: frontend-deployment
  ports:
  - port: 80
    targetPort: 80
    nodePort: 31927
